---
import { defaultLocale, getContent, type Locale } from "@/i18n/content";

interface Props {
  locale?: Locale;
  id?: string;
}

const { locale = defaultLocale, id = "contact" } = Astro.props;
const content = getContent(locale);
const page = content.pages.contact;
---

<section id={id} class="section-shell content-section">
  <div class="section-intro">
    <p class="eyebrow">{page.eyebrow}</p>
    <h2>{page.heading}</h2>
    <p>{page.lead}</p>
  </div>

  <div class="contact-grid">
    {
      content.profile.links.map((link) => (
        <a class="contact-link" href={link.href}>
          <span>{link.label}</span>
          <strong>{link.href.replace("mailto:", "")}</strong>
        </a>
      ))
    }
  </div>
</section>
